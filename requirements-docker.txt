# Pip-installable deployment dependencies for the web app (api/, app.py).
# Separate from environment.yml, which is the full conda dev/training
# environment -- this is only what the app needs at inference time.
#
# CPU-only torch: chemprop pulls in torch as a dependency, and the default
# PyPI wheel bundles CUDA, which is unnecessary weight on a CPU-only web
# host and roughly 10x the download size. Pinned >=2.6: older CPU wheels
# were built against numpy 1.x and crash under numpy>=2 (same issue noted
# in environment.yml for the conda build).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6

# Web frameworks
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.32

# Data
pandas>=2.0
numpy>=2.0

# Chemistry
rdkit>=2023.09

# Models used by the app: Random Forest (scikit-learn + mapie for the
# conformal prediction interval) and Chemprop / CheMeleon (chemprop).
scikit-learn>=1.3
mapie>=0.8
chemprop>=2.2
joblib

# Streamlit -> API mode
requests>=2.31
